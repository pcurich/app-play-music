import { OrderListPipe } from './order-list-pipe';

describe('OrderListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderListPipe();
    expect(pipe).toBeTruthy();
  });
});
